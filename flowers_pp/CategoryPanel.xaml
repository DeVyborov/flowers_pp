<UserControl x:Class="flowers_pp.CategoryPanel"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:flowers_pp"
        mc:Ignorable="d"
        Height="400" Width="280">
    <Border Background="White" x:Name="back" CornerRadius="20" Margin="5">
        <Grid>
            <Button Style="{StaticResource visible_button}" Name="btn_basket" Cursor="Hand" Height="370" Width="250" HorizontalAlignment="Center" VerticalAlignment="Center">
                <Grid>
                    <Image x:Name="photo_category" Source="/Assets/top-view-pink-flower-with-drops.jpg" Margin="0 0 0 200" Stretch="Fill" />
                    <TextBlock x:Name="title_category" VerticalAlignment="Bottom" Background="Transparent" Text="Свежесрезанные цветы" FontFamily="Montserrat" FontSize="16" Foreground="Black" TextAlignment="Center"  Cursor="Hand"/>
                </Grid>
            </Button>
        </Grid>
    </Border>
</UserControl>
